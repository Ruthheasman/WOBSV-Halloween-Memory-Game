[Skip to main content](https://docs.handcash.io/v3/connect/authentication#content-area)

[Handcash Developer Documentation home page![light logo](https://mintcdn.com/handcash/q03sWpGtEzxBOQeb/logo/handcash_overLight.svg?fit=max&auto=format&n=q03sWpGtEzxBOQeb&q=85&s=d4cd1f8e20eeafa1f19b8b30d4607056)![dark logo](https://mintcdn.com/handcash/q03sWpGtEzxBOQeb/logo/handcash_overDark.svg?fit=max&auto=format&n=q03sWpGtEzxBOQeb&q=85&s=6a44183051d60371ec6293e356511c58)](https://docs.handcash.io/)

Search...

Ctrl K

Search...

Navigation

v3 / Connect

Authentication

On this page

- [HandCash Authentication Integration Guide](https://docs.handcash.io/v3/connect/authentication#handcash-authentication-integration-guide)
- [Prerequisites](https://docs.handcash.io/v3/connect/authentication#prerequisites)
- [Configure Callback URLs](https://docs.handcash.io/v3/connect/authentication#configure-callback-urls)
- [Key Generation](https://docs.handcash.io/v3/connect/authentication#key-generation)
- [Required Dependency](https://docs.handcash.io/v3/connect/authentication#required-dependency)
- [Generate Key Pair](https://docs.handcash.io/v3/connect/authentication#generate-key-pair)
- [Authentication Flow](https://docs.handcash.io/v3/connect/authentication#authentication-flow)
- [Step 1: Generate Keys](https://docs.handcash.io/v3/connect/authentication#step-1%3A-generate-keys)
- [Step 2: Store Private Key](https://docs.handcash.io/v3/connect/authentication#step-2%3A-store-private-key)
- [Step 3: Redirect to HandCash](https://docs.handcash.io/v3/connect/authentication#step-3%3A-redirect-to-handcash)
- [Step 4: Handle Callback](https://docs.handcash.io/v3/connect/authentication#step-4%3A-handle-callback)
- [Step 5: Store Private Key for Session](https://docs.handcash.io/v3/connect/authentication#step-5%3A-store-private-key-for-session)
- [How It Works](https://docs.handcash.io/v3/connect/authentication#how-it-works)
- [Check User Permissions](https://docs.handcash.io/v3/connect/authentication#check-user-permissions)
- [Error Handling](https://docs.handcash.io/v3/connect/authentication#error-handling)
- [State Parameter Security](https://docs.handcash.io/v3/connect/authentication#state-parameter-security)
- [What This Means in Practice](https://docs.handcash.io/v3/connect/authentication#what-this-means-in-practice)
- [1️⃣ App Generates State When Starting Connect](https://docs.handcash.io/v3/connect/authentication#1%EF%B8%8F%E2%83%A3-app-generates-state-when-starting-connect)
- [2️⃣ Wallet Requires State](https://docs.handcash.io/v3/connect/authentication#2%EF%B8%8F%E2%83%A3-wallet-requires-state)
- [3️⃣ Wallet Redirects Back with Same State](https://docs.handcash.io/v3/connect/authentication#3%EF%B8%8F%E2%83%A3-wallet-redirects-back-with-same-state)
- [4️⃣ App Validates State](https://docs.handcash.io/v3/connect/authentication#4%EF%B8%8F%E2%83%A3-app-validates-state)
- [Security Best Practices](https://docs.handcash.io/v3/connect/authentication#security-best-practices)

# [​](https://docs.handcash.io/v3/connect/authentication\#handcash-authentication-integration-guide)  HandCash Authentication Integration Guide

## [​](https://docs.handcash.io/v3/connect/authentication\#prerequisites)  Prerequisites

1. **Create an application** in the [Developer Dashboard](https://dashboard.handcash.io/)
2. **Configure callback URLs** in your application settings
3. **Get your credentials** (`AppId` and `AppSecret`)

## [​](https://docs.handcash.io/v3/connect/authentication\#configure-callback-urls)  Configure Callback URLs

Configure callback URLs in [dashboard.handcash.io](https://dashboard.handcash.io/):

1. Select your application
2. Go to **Settings** or **Configuration**
3. Set:
   - **Authorization Success URL**: Where users redirect after authorization (e.g., `https://yourapp.com/auth/success`)
   - **Authorization Failed URL**: Where users redirect if they decline (e.g., `https://yourapp.com/auth/failed`)

**Important:** Use HTTPS in production. For local development, `http://localhost` is allowed.

## [​](https://docs.handcash.io/v3/connect/authentication\#key-generation)  Key Generation

### [​](https://docs.handcash.io/v3/connect/authentication\#required-dependency)  Required Dependency

Copy

```
"@noble/secp256k1": "2.2.3"
```

### [​](https://docs.handcash.io/v3/connect/authentication\#generate-key-pair)  Generate Key Pair

Copy

```
import * as secp256k1 from "@noble/secp256k1"

export function generateAuthenticationKeyPair() {
  const privateKey = secp256k1.utils.randomSecretKey()
  const publicKey = secp256k1.getPublicKey(privateKey, true)

  return {
    privateKey: Buffer.from(privateKey).toString("hex"),
    publicKey: Buffer.from(publicKey).toString("hex"),
  }
}
```

## [​](https://docs.handcash.io/v3/connect/authentication\#authentication-flow)  Authentication Flow

### [​](https://docs.handcash.io/v3/connect/authentication\#step-1:-generate-keys)  Step 1: Generate Keys

Copy

```
const { privateKey, publicKey } = generateAuthenticationKeyPair()
```

### [​](https://docs.handcash.io/v3/connect/authentication\#step-2:-store-private-key)  Step 2: Store Private Key

Store the private key securely:**Client:**`localStorage`, `sessionStorage`, or in-memory

**Server:** Database, Redis, session, or JWT

Copy

```
// Client example
sessionStorage.setItem('handcash_private_key', privateKey)

// Server example
await db.users.update(userId, { handcashPrivateKey: privateKey })
```

### [​](https://docs.handcash.io/v3/connect/authentication\#step-3:-redirect-to-handcash)  Step 3: Redirect to HandCash

Copy

```
const authUrl = `https://handcash.io/connect?appId=YOUR_APP_ID&publicKey=${publicKey}`
window.location.href = authUrl
```

The user will log in and authorize your app on HandCash.

**Recommended: Use State Parameter for CSRF Protection**For enhanced security, include a `state` parameter to prevent CSRF attacks. See the [State Parameter Security](https://docs.handcash.io/v3/connect/authentication#state-parameter-security) section below.

### [​](https://docs.handcash.io/v3/connect/authentication\#step-4:-handle-callback)  Step 4: Handle Callback

After authorization, HandCash redirects to your **Authorization Success URL**. Retrieve the stored private key and validate:

Copy

```
import { getInstance, Connect } from '@handcash/sdk'

const privateKey = sessionStorage.getItem('handcash_private_key') // Retrieve stored key

const sdk = getInstance({ appId: 'YOUR_APP_ID', appSecret: 'YOUR_APP_SECRET' })
const client = sdk.getAccountClient(privateKey)

// Validate by fetching user profile
const { data: profile } = await Connect.getCurrentUserProfile({ client })
console.log('User authenticated:', profile.publicProfile.handle)
```

### [​](https://docs.handcash.io/v3/connect/authentication\#step-5:-store-private-key-for-session)  Step 5: Store Private Key for Session

Associate the private key with the user’s session:

Copy

```
req.session.handcashPrivateKey = privateKey
// or
await db.users.update(userId, { handcashPrivateKey: privateKey })
```

## [​](https://docs.handcash.io/v3/connect/authentication\#how-it-works)  How It Works

1. Generate a private/public key pair
2. Send the public key to HandCash during authorization
3. User authorizes your app on HandCash
4. Use the private key for all API calls - it serves as your authentication credential
5. HandCash validates the private key matches the authorized public key

## [​](https://docs.handcash.io/v3/connect/authentication\#check-user-permissions)  Check User Permissions

Copy

```
const { data } = await Connect.getPermissions({ client })
console.log('User granted permissions:', data?.items)
```

## [​](https://docs.handcash.io/v3/connect/authentication\#error-handling)  Error Handling

Copy

```
try {
  const client = sdk.getAccountClient(privateKey)
  const profile = await Connect.getCurrentUserProfile({ client })
} catch (error) {
  if (error.message.includes('Invalid token')) {
    // Invalid private key, redirect to re-authenticate
    const { publicKey } = generateAuthenticationKeyPair()
    window.location.href = `https://handcash.io/connect?appId=${appId}&publicKey=${publicKey}`
  }
}
```

## [​](https://docs.handcash.io/v3/connect/authentication\#state-parameter-security)  State Parameter Security

**Recommended:** Use the `state` parameter to prevent CSRF attacks and ensure the callback originated from your app session.

### [​](https://docs.handcash.io/v3/connect/authentication\#what-this-means-in-practice)  What This Means in Practice

#### [​](https://docs.handcash.io/v3/connect/authentication\#1%EF%B8%8F%E2%83%A3-app-generates-state-when-starting-connect)  1️⃣ App Generates State When Starting Connect

Generate a random, unguessable string and store it in your app session:

Copy

```
import crypto from 'crypto'

// Generate random state
const state = crypto.randomBytes(32).toString('hex')

// Store in session (server-side example)
req.session.handcashState = state

// Or client-side (sessionStorage)
sessionStorage.setItem('handcash_state', state)
```

Include the state in the redirect URL:

Copy

```
const authUrl = `https://handcash.io/connect?appId=YOUR_APP_ID&publicKey=${publicKey}&state=${state}`
window.location.href = authUrl
```

#### [​](https://docs.handcash.io/v3/connect/authentication\#2%EF%B8%8F%E2%83%A3-wallet-requires-state)  2️⃣ Wallet Requires State

The HandCash wallet will:

- Require `state` to be present in the authorization request
- Echo the same `state` back in the callback URL
- Treat `state` as proof that the key came from the same app session that initiated connect

#### [​](https://docs.handcash.io/v3/connect/authentication\#3%EF%B8%8F%E2%83%A3-wallet-redirects-back-with-same-state)  3️⃣ Wallet Redirects Back with Same State

After authorization, HandCash redirects to your callback URL with the state included:

Copy

```
https://yourapp.com/auth/success?userId=...&publicKey=...&state=STATE
```

#### [​](https://docs.handcash.io/v3/connect/authentication\#4%EF%B8%8F%E2%83%A3-app-validates-state)  4️⃣ App Validates State

Validate that the state matches the one stored in your session:

Copy

```
// Server-side example
app.get('/auth/success', async (req, res) => {
  const callbackState = req.query.state
  const sessionState = req.session.handcashState

  // Validate state matches
  if (callbackState !== sessionState) {
    return res.status(403).send('Invalid state parameter - possible CSRF attack')
  }

  // Clear state from session
  delete req.session.handcashState

  // Continue with authentication...
  const privateKey = req.session.handcashPrivateKey
  // ... rest of authentication flow
})
```

Copy

```
// Client-side example
const urlParams = new URLSearchParams(window.location.search)
const callbackState = urlParams.get('state')
const sessionState = sessionStorage.getItem('handcash_state')

if (callbackState !== sessionState) {
  console.error('Invalid state parameter - possible CSRF attack')
  return
}

// Clear state
sessionStorage.removeItem('handcash_state')

// Continue with authentication...
```

**If state mismatch → reject the request** \- This indicates a potential CSRF attack.

## [​](https://docs.handcash.io/v3/connect/authentication\#security-best-practices)  Security Best Practices

- **Use state parameter** \- Always include and validate `state` to prevent CSRF attacks
- **Store private keys securely** \- Use encrypted storage or secure sessions
- **Use HTTPS** \- Always use HTTPS for redirect URLs
- **Validate private keys** \- Check validity before making API calls
- **Protect the private key** \- Treat it as sensitive authentication data

[Getting Started](https://docs.handcash.io/v3/getting-started) [User Account Creation](https://docs.handcash.io/v3/connect/user-creation)

Ctrl+I

Assistant

Responses are generated using AI and may contain mistakes.